spring:
  data:
    rest:
      basePath: /api
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: santa
    password: hohoho

validation:
  minGiftCost: 0
  minPartyMembers: 3